@inject ClientDataService clientDataService

<h3>Añadir cliente</h3>
<EditForm Model=@client OnSubmit=@Addclient>
  <DataAnnotationsValidator/>
  <div class="form-group">
    <label for="Name">Nombre</label>
    <InputText @bind-Value=client.Name class="form-control" id="Name" />
  </div>
  <div class="form-group">
    <label for="email">Correo</label>
    <InputText @bind-Value=client.Email class="form-control" id="email" />
  </div>
  <div class="form-group">
    <label for="phone">Telefono</label>
    <InputText @bind-Value=client.Phone class="form-control" id="phone" />
  </div>
    <div class="form-group">
    <label for="phone">Telefono</label>
    <InputText @bind-Value=client.Phone class="form-control" id="phone" />
  </div>
  <input type="submit" class="btn btn-primary" value="Save"/>
</EditForm>
@code {
    private AddClientRequestDto client = new AddClientRequestDto();

    private void Addclient()
    {
        clientDataService.AddClient(client);
    }

}
